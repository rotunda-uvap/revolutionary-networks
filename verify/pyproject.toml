[project]
name = "revnet-verify"
dependencies = [
    "numpy>=2",
    "pandas>=3"
]
requires-python = ">= 3.12"
dynamic = ["version"]

[project.scripts]
revnet-verify = "revnet_verify.cmd:main"

[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = {attr = "revnet_verify.__version__"}